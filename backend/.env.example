# Server port
PORT=8080

# Authentication password (CHANGE THIS!)
AUTH_TOKEN=change_this_password

# Storage directory (where files are saved)
WATCH_DIR=./uploads

# Storage quota in GB (50-1000)
STORAGE_QUOTA_GB=100
